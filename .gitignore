# Rust
/target/
Cargo.lock.bak
**/*.rs.bk
*.pdb

# Lambda artifacts
/target/lambda/

# Claude Code / agent local state
.claude/
sync-prompts.sh

# Ralph workspace (artefatos copiados para docs/; fonte viva fica local)
.ralph/

# Artefatos dos gates de qualidade (lefthook pre-push): cargo-mutants e
# cargo-llvm-cov escrevem na raiz do repo. São saída de ferramenta, não fonte.
/mutants.out/
/mutants.out.old/
/coverage.lcov

# IDE
.idea/
.vscode/
*.iml
.DS_Store

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key
# Exceção deliberada e estreita: pares de chave descartáveis, gerados só para a
# suíte de testes de RS256/ES256. Não são segredo e nunca protegeram nada — ver
# serverust-auth/tests/fixtures/README.md. O `*.pem` acima segue valendo em
# todo o resto do repositório.
!serverust-auth/tests/fixtures/*.pem
